import React from 'react';

const Integrations = () => {
  return (
    <div className="max-w-3xl mx-auto py-10 px-4 sm:px-6">
      <h1 className="text-2xl sm:text-3xl font-semibold text-gray-900 mb-3">Integrations</h1>
      <p className="text-sm text-gray-600 mb-4">
        FRYLY is intentionally lightweight today and focuses on being the shared home for everything your group tracks together.
      </p>
      <p className="text-sm text-gray-600">
        Over time we plan to explore simple exports and calendar-style hooks, but the product is designed so that most everyday organising can happen directly inside groups without needing a long list of external tools.
      </p>
    </div>
  );
};

export default Integrations;
